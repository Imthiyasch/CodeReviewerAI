{
  "name": "cr42-root",
  "private": true,
  "scripts": {
    "build": "npm install --prefix server && npm run db:generate --prefix server && npm install --prefix client && npm run build --prefix client"
  }
}
